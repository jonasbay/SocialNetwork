@model SocialNetwork.Models.User

@{
    ViewData["Title"] = "FollowUser";
}

<h1>FollowUser</h1>

<h4>User</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="FollowUser">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserToFollow" class="control-label"></label>
                <input asp-for="UserToFollow" class="form-control" />
                <span asp-validation-for="UserToFollow" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
